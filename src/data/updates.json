{
  "$schema": "./updates.schema.json",
  "version": "0.3.9",
  "releasedAt": "2026-09-01",
  "whatsNewKey": "2026-09-mlflow-integration",
  "meta": {
    "eyebrow": "Latest release",
    "title": "What's new in Stormlog",
    "description": "MLflow integration for experiment tracking and model registry integration, mirroring Weights & Biases exporter capabilities for unified ML workflow observability."
  },
  "updates": [
    {
      "id": "mlflow-exporter",
      "kicker": "MLflow integration",
      "title": "MLflow experiment tracking exporter",
      "summary": "Stormlog now exports memory profiles and profiling metadata to MLflow, enabling integrated tracking of GPU memory alongside model metrics, parameters, and artifacts in your MLflow experiments.",
      "highlights": [
        "Log memory traces and summary statistics directly to MLflow runs",
        "Automatic model registry integration for memory profiles",
        "Feature parity with Weights & Biases exporter for experiment tracking",
        "Tag and filter runs by memory profiles in MLflow UI"
      ],
      "command": "pip install \"stormlog[mlflow]>=0.3.9\"",
      "codeLabel": "python",
      "code": "from stormlog.exporters import MLflowExporter\nfrom stormlog.pytorch import GPUMemoryProfiler\nimport mlflow\n\nmlflow.start_run()\n\nprofiler = GPUMemoryProfiler()\nexporter = MLflowExporter()\n\nwith profiler.profile_context(\"training\"):\n    # Your training loop\n    for batch in dataloader:\n        model(batch).backward()\n\nexporter.export(profiler.trace(), \"gpu_memory_profile\")\nmlflow.end_run()",
      "href": "#whats-new",
      "externalLink": "https://github.com/Silas-Asamoah/stormlog/pull/207"
    }
  ]
}
